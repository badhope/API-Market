# =============================================================================
# API-Market environment variables - TEMPLATE ONLY
# =============================================================================
#
# SECURITY NOTICE:
#   - DO NOT put real API keys, tokens, or passwords in this file.
#   - This file is tracked by git for documentation purposes.
#   - Copy to .env (which is gitignored) and fill in real values locally.
#   - In production, set these via your host's secret store
#     (GitHub Actions secrets, Cloudflare secrets, etc.)
# =============================================================================

# Override the GitHub Pages base path (default: "" — site is served at the
# project root). Set to "/<repo-name>" for project-page hosting.
NEXT_PUBLIC_BASE_PATH=
