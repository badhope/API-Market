# =============================================================================
# API-Market scripts dependencies
# =============================================================================
# This file is kept for compatibility with upstream tooling conventions and
# the data validation scripts. The main API-Market backend now uses the
# dependencies pinned in `backend/pyproject.toml` (FastAPI, SQLAlchemy 2.0
# async, pydantic-settings, etc.). Data collection and validation scripts
# in this directory use the packages below.
# =============================================================================

# HTTP requests (used by validate/links.py and the legacy pipeline)
requests>=2.27.0
urllib3>=1.26.0

# Async HTTP (used by scripts/pipeline.py and pipeline/collector.py)
aiohttp>=3.8.0
aiofiles>=0.8.0

# Data validation (used by validate/format.py and validate/links.py)
jsonschema>=4.0.0

# Date parsing
python-dateutil>=2.8.0

# -----------------------------------------------------------------------
# The following packages were previously listed here but are no longer
# used by API-Market (we use ruff instead of flake8/black, and mypy is
# run via the backend's dev dependencies):
#   - flake8
#   - black
#   - mypy
#   - pytest / pytest-asyncio / pytest-cov
# -----------------------------------------------------------------------
